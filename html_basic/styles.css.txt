/* Apply Flexbox to the body container */
body {
    display: flex;
    flex-direction: column; /* Arrange header, main, and footer vertically */
    height: 100vh; /* Ensure the body takes the full height of the viewport */
    margin: 0; /* Remove default body margin */
}

/* Apply Flexbox to the main container */
main {
    display: flex;
    flex-direction: row; /* Arrange article and aside horizontally */
    flex: auto; /* Allow main to take up remaining space */
}

/* Article should take up 2/3 of the width */
article {
    flex: 2; /* Takes up 2 boxes */
    overflow-y: auto; /* Make article scrollable */
    padding: 1rem; /* Optional padding for aesthetics */
}

/* Aside should take up 1/3 of the width */
aside {
    flex: 1; /* Takes up 1 box */
    overflow-y: auto; /* Make aside scrollable */
    padding: 1rem; /* Optional padding for aesthetics */
}

/* Styling for header and footer (optional) */
header, footer {
    background-color: #f4f4f4; /* Light gray background */
    padding: 1rem;
    text-align: center;
}

/* Ensure content has space for scrolling */
article, aside {
    height: calc(100vh - 2rem); /* Adjust height for scrollable content */
}
